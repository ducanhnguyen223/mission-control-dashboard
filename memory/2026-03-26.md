# Memory — 2026-03-26

## Gateway OOM / heartbeat chết cả đêm
- Nguyên nhân: `openclaw-gateway` bị OOM kill lúc 02:34 UTC do **Discord crash-loop** (`Failed to resolve Discord application id`) làm memory phình tới 1.1G
- Hậu quả: heartbeat không chạy từ khoảng 18:00 UTC ngày 25/3 đến 02:34 UTC ngày 26/3
- Fix: tắt `channels.discord.enabled = false` và `plugins.entries.discord.enabled = false` trong `/root/.openclaw/openclaw.json`
- Restart gateway lúc 03:05 UTC → heartbeat `started` lại lúc 03:06 UTC, xác nhận sạch lúc 03:44 UTC
- Discord đã im, không còn crash-loop trong log

## HEARTBEAT.md workflow cập nhật
- Đã thêm fallback: heartbeat liếc tmux session `claude`, nếu đủ rõ ngữ cảnh thì tự đẩy prompt tiếp để dashboard tiến triển xuyên đêm
- Workflow: check tmux → đọc output → nghĩ prompt tiếp → gửi vào session `claude`
- Mục tiêu: giúp Mission Control Dashboard tự build qua đêm không cần user canh

## Mission Control Dashboard — trạng thái build
- tmux session `claude` đang chạy với `gpt-5.3-codex` (alias sonnet)
- CWD: `/root/.openclaw/workspace/mission-control-dashboard`
- Tiến độ khi check: đã scaffold Next.js, đang sửa lint/package ESLint version conflict
- Bị dừng tạm ở bước Claude Code xin approve lệnh `node -e "..."`
- Cần tiếp tục build: approve và đẩy prompt dùng ECC + frontend-design plugin

## Reminder: cần làm
- Gửi prompt vào tmux `claude` nhắc rõ dùng ECC và frontend-design plugin khi nhịp heartbeat kế tiếp
- Xác nhận heartbeat thực sự nổ sau 03:06 UTC (nhịp 30m)
- Theo dõi build dashboard tiến triển, báo khi milestone rõ

## Preference mới
- Khi điều khiển Claude Code / tmux session `claude`, nếu gặp prompt xin quyền/approve trong phạm vi workflow build-dev nội bộ thì ưu tiên auto-accept thay vì đứng chờ hỏi lại user
- Với prompt yes/no của Claude trong workflow nội bộ, mặc định chọn `Yes`
- Ngoại lệ: không tự approve các hành động phá hủy rõ ràng hoặc tác động ra ngoài
- Không tự kill tmux session `claude` nữa, trừ khi user yêu cầu rõ ràng
- Dùng watchdog riêng để giám sát tmux `claude`, không phụ thuộc heartbeat cho việc auto-control prompt
- Các action push / external / destructive vẫn để OpenClaw kiểm soát ở lớp điều phối, không giao mặc định cho Claude tự quyết

## OpenClaw config thay đổi
- `channels.discord.enabled`: true → **false**
- `plugins.entries.discord.enabled`: true → **false**
- Config path: `/root/.openclaw/openclaw.json`
- Gateway pid mới: `165422` (restart lần 1 sau OOM), sau đó `169583` (restart lần 2 sau fix Discord)
